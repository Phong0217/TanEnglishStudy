import AppShell from '@/Layouts/AppShell';import {Badge,Card,EmptyState,PageHeader,Pagination} from '@/Components/ui';import {Head} from '@inertiajs/react';type Student={id:number;name:string;email:string;status:string;student_profile?:{student_code:string}};export default function Students({students}:{students:{data:Student[];links:Array<{url:string|null;label:string;active:boolean}>}}){return <AppShell><Head title="My Students"/><PageHeader title="My Students" description="Học sinh đã đăng ký trong các lớp bạn đang phụ trách."/><Card className="mt-6">{students.data.length?<div className="divide-y divide-slate-100">{students.data.map(s=><div key={s.id} className="flex items-center justify-between gap-3 p-5"><div><p className="font-medium">{s.name}</p><p className="text-sm text-slate-500">{s.student_profile?.student_code} · {s.email}</p></div><Badge value={s.status}/></div>)}</div>:<div className="p-5"><EmptyState title="Không tìm thấy học sinh" description="Học sinh trong các lớp đang hoạt động sẽ xuất hiện ở đây."/></div>}<Pagination links={students.links}/></Card></AppShell>}

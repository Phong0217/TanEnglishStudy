import AppShell from '@/Layouts/AppShell';import {Badge,Card,EmptyState,PageHeader,Pagination} from '@/Components/ui';import {Head,Link,router,useForm,usePage} from '@inertiajs/react';import {FormEvent} from 'react';import {PageProps} from '@/types';import {Plus} from 'lucide-react';
type Question={id:number;type:string;skill:string;difficulty:string;status:string;usage_count:number;ai_generation_job_id?:number;versions:Array<{id:number;content_json:{prompt?:string};explanation?:string}>;creator:{name:string}};export default function Questions({questions}:{questions:{data:Question[];links:Array<{url:string|null;label:string;active:boolean}>}}){const {auth}=usePage<PageProps>().props;const prefix=auth.role==='ADMIN'?'/admin':'/teacher';const form=useForm({type:'multiple_choice',skill:'READING',difficulty:'MEDIUM',content:{prompt:'',options:[{id:'a',text:''},{id:'b',text:''},{id:'c',text:''}]},answer_key:{correctOptionId:'a'},settings:{},explanation:'',rubric:null as null,grading_mode:'AUTO'});const submit=(e:FormEvent)=>{e.preventDefault();form.post(`${prefix}/questions`,{onSuccess:()=>form.reset()});};return <AppShell><Head title="Ngân hàng câu hỏi"/><PageHeader title="Ngân hàng câu hỏi" description="Versioned, reviewable assessment items with immutable usage history." actions={<Link href={`${prefix}/ai-generator`} className="btn btn-primary">Tạo câu hỏi AI từ tài liệu</Link>}/><Card className="mt-6">{questions.data.length?<div className="divide-y divide-slate-100">{questions.data.map(q=><div key={q.id} className="p-5"><div className="flex flex-col gap-3 sm:flex-row sm:items-start sm:justify-between"><div><p className="font-medium text-slate-900">{q.versions[0]?.content_json.prompt||q.type.replaceAll('_',' ')}</p><p className="mt-1 text-sm text-slate-500">{q.type.replaceAll('_',' ')} · {q.skill} · {q.difficulty} · {q.usage_count} uses</p></div><div className="flex items-center gap-2"><Badge value={q.status}/>{q.ai_generation_job_id && <Link className="btn btn-outline btn-sm" href={`${prefix}/ai-jobs/${q.ai_generation_job_id}/review`}>Review / View source</Link>}{q.status==='IN_REVIEW'&&<><button className="btn-secondary px-3" onClick={()=>router.patch(`${prefix}/questions/${q.id}/review`,{status:'APPROVED'})}>Approve</button><button className="btn-secondary px-3 text-red-600" onClick={()=>router.patch(`${prefix}/questions/${q.id}/review`,{status:'REJECTED',reason:'Content requires revision.'})}>Reject</button></>}</div></div></div>)}</div>:<div className="p-5"><EmptyState title="No questions found" description="Create a question manually or generate grounded drafts from documents."/></div>}<Pagination links={questions.links}/></Card><Card id="create-question" className="mt-6 p-5"><h2 className="section-title">Create multiple-choice question</h2><p className="muted mt-1">This editor enforces three non-empty options and one correct answer.</p><form onSubmit={submit} className="mt-5 space-y-4"><label><span className="label">Prompt *</span><input className="field" value={form.data.content.prompt} onChange={e=>form.setData('content',{...form.data.content,prompt:e.target.value})} required/></label><div className="grid gap-3 md:grid-cols-3">{form.data.content.options.map((option,index)=><label key={option.id}><span className="label">Option {option.id.toUpperCase()} *</span><input className="field" value={option.text} onChange={e=>form.setData('content',{...form.data.content,options:form.data.content.options.map((o,i)=>i===index?{...o,text:e.target.value}:o)})} required/></label>)}</div><div className="grid gap-4 sm:grid-cols-3"><label><span className="label">Correct option</span><select className="field" value={form.data.answer_key.correctOptionId} onChange={e=>form.setData('answer_key',{correctOptionId:e.target.value})}>{form.data.content.options.map(o=><option key={o.id} value={o.id}>{o.id.toUpperCase()}</option>)}</select></label><label><span className="label">Skill</span><select className="field" value={form.data.skill} onChange={e=>form.setData('skill',e.target.value)}>{['READING','LISTENING','VOCABULARY','GRAMMAR','WRITING','SPEAKING','MIXED'].map(v=><option key={v}>{v}</option>)}</select></label><label><span className="label">Độ khó</span><select className="field" value={form.data.difficulty} onChange={e=>form.setData('difficulty',e.target.value)}>{['EASY','MEDIUM','HARD'].map(v=><option key={v}>{v}</option>)}</select></label></div><button className="btn-primary" disabled={form.processing}>Save draft question</button></form></Card></AppShell>}
